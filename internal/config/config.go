package config

const (
	SocketPath = "/tmp/boxyd.sock"

	HOME = "/home/ox03bb" //"$HOME"

	ProjectPath = HOME + "/Desktop/boxy"

	RegistryPath = ProjectPath + "/registry"
	EnvPath      = ProjectPath + "/env"
)
